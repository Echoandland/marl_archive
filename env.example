# Example environment variables (do NOT commit real secrets)
#
# Usage:
#   cp env.example .env   # optional, if you use a dotenv loader
#   export $(grep -v '^#' env.example | xargs)  # quick-and-dirty
#
# NOTE: This repo reads credentials from environment variables only.

# OpenAI (for gpt-*/o3-* models via the OpenAI SDK)
OPENAI_API_KEY=
# Optional: point OpenAI SDK to a compatible endpoint (e.g., local vLLM)
# OPENAI_BASE_URL=http://localhost:8000/v1
# Optional: extra header for certain gateways/proxies
# X_API_KEY=

# Google Gemini (google-genai)
# Either provide a single key:
GEMINI_API_KEY=
# Or multiple keys (comma-separated) for simple rotation:
# GEMINI_API_KEYS=key1,key2,key3

# DashScope (Qwen3 via OpenAI-compatible endpoint)
DASHSCOPE_API_KEY=
# DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

